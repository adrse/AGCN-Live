// Empty by default: the Python server hosts the site and API on one origin.
// Set an HTTPS backend URL only when an external static host is intentional.
window.AGCNLIVE_API_BASE = window.AGCNLIVE_API_BASE || "";
